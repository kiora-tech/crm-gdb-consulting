{% extends 'base.html.twig' %}

{% block title %}DocumentType{% endblock %}

{% block body %}
    <h1>DocumentType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_type.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ document_type.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_document_type_index') }}">back to list</a>

    <a href="{{ path('app_document_type_edit', {'id': document_type.id}) }}">edit</a>

    {{ include('document_type/_delete_form.html.twig') }}
{% endblock %}
