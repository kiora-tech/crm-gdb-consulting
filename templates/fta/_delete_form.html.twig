<form method="post" action="{{ path('app_fta_delete', {'id': fta.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fta.id) }}">
    <button class="btn">Delete</button>
</form>
